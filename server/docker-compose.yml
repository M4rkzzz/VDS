services:
  signaling:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vds-signaling
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      PORT: 3000
      DISCONNECT_GRACE_MS: 30000
